# ロードマップ / タスク管理

更新日: 2026-08-16（Yellow Pages ページのカード行化まで）

状態: 未着手 / 進行中 / 完了。完了したタスクは 1 行に畳んで「完了」節へ移す。

## 未着手

仕様書作成時に見つかった実装のズレ。**今は手を付けない**方針。
詳細と根拠は [investigations/implementation-gaps.md](investigations/implementation-gaps.md)。

| # | 内容 | 影響 | 参照 |
|---|---|---|---|
| 3 | UI で編集できるが未適用の設定が多数（`Display.*` 全項目、`Network.ProxyUrl` / `UserAgent`、`Notifications.BalloonTimeoutSeconds` / `SoundEnabled` / `SoundFile`、`Behavior.ActiveFilterIndex`、`Window.X` / `Y`） | 設定しても何も変わらない | [gaps 2](investigations/implementation-gaps.md#2-ui-で編集できるのに反映されない設定) |

3 は「実装する」か「UI と設定項目から消す」かの判断が先に必要。

### 実装中に見つかったもの

| # | 内容 | 影響 |
|---|---|---|
| 7 | 同じチャンネルが複数の YP に出ていても統合しない | 一覧に同じ番組が YP の数だけ並ぶ |

7 は `AutoRefreshService` が全 YP のチャンネルを連結するだけで、`Id` による重複除去をしないため。
「先に取得した YP を優先」なのか「両方出して YP 名で区別」なのかを決めていないので未着手。

## タグ方式の残り

| # | 内容 | 備考 |
|---|---|---|
| A | ルールのインポート / エクスポート | モックにはあるが今回の指示に無いため未実装 |
| B | 自動ダウンロードをタグ方式へ寄せる（「タグが付いたら録音」） | 現状は独立したルール形式のまま |
| C | 星ルールを別リスト（購読）で持ち、判定時にルールへ展開する | 現状は `IsAuto` フラグで区別するだけ |
| D | 手書きルールでお気に入りタグが付く場合、星をオフにしても消えない | 仕様どおりだが UI に説明が無い |

## 設定画面リニューアルの残り

第1弾で左ナビとカード行のパターンを確立した。第2弾以降はこれを横展開する。

| # | 内容 | 備考 |
|---|---|---|
| S2 | ダウンロードページをカード行へ | 自動ダウンロードルールはタグ機能へ吸収予定（タグ方式の残り B） |
| S3 | 表示ページをカード行へ | 色設定はフィルタ/タグ機能のリニューアルで吸収される |
| S4 | 通知ページをカード行へ | 「お気に入りチャンネルを通知」もタグ機能に吸収されるまでの暫定 |
| S5 | ネットワークページを「詳細」へ改称するか判断 | 検討中 |
| S6 | 一覧内のチェックボックスをトグルスイッチへ寄せるか判断 | 現状は単独の設定だけトグル。一覧の行は複数選択の意味なのでチェックのまま |

## 進行中

なし。

## 完了

- 2026-08-16 YP ごとの絞り込み（ビットレート上下限・タイプの正規表現）を削除。
  タグとビューで同じことができる上、取得側の絞り込みは一覧に載る前に落とすので
  絞られたことが画面に出ず、「出てこない」ときに疑う導線が無かった。
  旧 gap 6（不正な正規表現にエラー表示が無い）は設定項目ごと無くなって解消
- 2026-08-16 S1: Yellow Pages ページをカード行へ。固定高のリスト + 常設の編集フォームをやめ、
  「⋯」メニューから開く `YpServerEditDialog` に寄せた。
  併せて `IYpServerStateService` を追加して YP ごとの実行時状態の置き場を作り、
  カード行の 3 段目に「21:32:05 更新 ・ 63 件」「取得できません: ...」を出すようにした（旧 gap 2 を解消）。
  タイムアウトを呼び出し側のキャンセルと別に扱い、`HttpClient` 固定の 10 秒も外した
- 2026-08-16 自動更新を設定値どおりの挙動にした。取得側の「4 分未満はスキップ」ガード
  （初版から入っていたが発動していなかった）を撤去し、実アクセス間隔 = 設定した更新間隔に統一。
  YP への負荷はプリセットの下限だけで抑える形にし、選択肢を 60 / 120 / 300 秒へ変更（30 秒は削除）。
  旧 gap 1 はこれで解消。残った `LastError` / `ChannelCount` の問題は gap 6 へ切り出した
- 2026-08-16 録画した FLV が単体で再生できない問題を修正。書き込み先を `IRecordingSink` として
  切り出し、FLV はタグを解析して組み直す `FlvRecordingSink` を通すようにした
  （ts を録画開始基準へ・再送ヘッダ除去・onMetaData 差し替え）。FLV 以外は従来どおり素通し。
  WMV(ASF) / MKV の同種の問題は未対応（[spec/recording.md](spec/recording.md#既知の制限)）
- 2026-08-16 プレイヤーをコンテンツタイプごとの設定へ作り替え。「既定 1 つ」をやめ、
  タイプ（FLV / MKV / … / その他）1 つにつき 1 件を持つ形にした。引数の置換子を
  `{stream}` `{channelname}` `{contact}` `{genre}` `{description}` `{comment}`
  `{contenttype}` `{direct}` の 8 つへ拡張し、編集ダイアログに置換子の一覧と「設定例」を追加。
  旧 `{url}` と旧形式（名前 + 既定フラグ）は起動時に自動移行（引き継ぐのは 1 件だけ）。
  実行時専用だった `PlayerItem` は廃止し `PlayerSettings` に一本化
- 2026-08-16 トレイ常駐を実装。`TrayIconService`（`Shell_NotifyIcon`）を追加し、
  「起動時の状態 = トレイに格納」「最小化ボタン = トレイに格納」が実際に動くようにした。
  設定の読み込みを `App.OnStartup` へ移動（ウィンドウを出す前に起動時の状態を知る必要があるため）。
  アプリのアイコン `Assets/app.ico` も新規作成
- 2026-08-16 設定ダイアログを `SettingsDraft`（複製）の編集方式に変更。OK / キャンセルは残したまま、
  キャンセルで全ページの変更が確実に戻るようにした。旧 gap 5 はこれで解消
- 2026-08-16 設定画面リニューアル 第1弾。左ナビを 6 カテゴリ（先頭に「全般」）へ組み替え、
  1 設定 = 1 カード行の共通コントロール `SettingCard` を用意。旧「プレイヤー」「動作」ページを
  「全般」へ統合。自動更新間隔は自由入力からプリセットへ。`OpenOnDoubleClick` は廃止（常にオン）
- 2026-08-15 お気に入り/フィルタをタグ方式へ作り替え。ルールはタグを付けるだけにし、
  色・通知・非表示はタグ側の属性へ移した。旧 `Favorites` は起動時に自動移行。
  ビュー欄・星・非表示件数バー・ルール編集・タグ設定を追加。
  これに伴い旧 gap 4（お気に入り編集が即座に反映されない）は解消
- 2026-08-15 ドキュメントを仕様 (`docs/spec/`) と設計理由 (`docs/design/`) に分離
