<UserControl
    x:Class="YPBrowser.Views.SettingsPages.DisplayPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer>
        <StackPanel Margin="4">
            <TextBlock Text="表示設定" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12" />

            <TextBlock Text="フォント" FontWeight="SemiBold" Margin="0,0,0,4" />
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="FontFamilyBox" Grid.Column="0" Margin="0,0,4,0"
                         TextChanged="FontFamilyBox_TextChanged"/>
                <TextBox x:Name="FontSizeBox" Grid.Column="1"
                         TextChanged="FontSizeBox_TextChanged"/>
            </Grid>

            <TextBlock Text="チャンネル名色（お気に入り）" Margin="0,0,0,2" />
            <TextBox x:Name="FavColorBox" Margin="0,0,0,12"
                     TextChanged="FavColorBox_TextChanged"/>

            <TextBlock Text="新着チャンネル色" Margin="0,0,0,2" />
            <TextBox x:Name="NewColorBox"
                     TextChanged="NewColorBox_TextChanged"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
